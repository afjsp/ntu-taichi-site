# ntu-taichi-site

臺大太極拳課程線上講義（GitHub Pages 靜態網站）。

⚠️ **這個 repo 裡的 HTML 全部是自動生成的，不要直接手改。**
內容修正請改來源要點檔（`04_台大太極/.../03_簡易要點/*.txt`），
再執行生成器重跑一次：

```bash
cd "太極課程/課程資料/台大網頁生成區"
python3 updater.py build
```

生成器與設定檔在工作區的 `太極課程/課程資料/台大網頁生成區/`（不在本 repo 內）。

與古池太極網站（`afjsp/taichi-site`）完全獨立，兩邊不共用程式碼也不共用資料。
